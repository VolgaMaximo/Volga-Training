'use client';
import Link from 'next/link';
export default function DonePage(){const score=typeof window!=='undefined'?Number(sessionStorage.getItem('volga_quiz_score')||0):0;const passed=score>=85;const oralDone=typeof window!=='undefined'?sessionStorage.getItem('volga_oral_done')==='1':false;return <main><div className="brand">VOLGA · РЕЗУЛЬТАТ</div><h1>{passed?(oralDone?'ЭКЗАМЕН ЗАВЕРШЁН':'ТЕОРИЯ СДАНА'):'НУЖНО ПОВТОРИТЬ'}</h1><div className="card"><div className="score">{score}%</div>{passed?(oralDone?<p className="success">Ответы сохранены и отправлены администратору на проверку.</p>:<p className="success">Теоретическая часть пройдена.</p>):<p className="warning">Минимум — 85%. Новую попытку сможет разрешить администратор после разбора ошибок.</p>}<div style={{height:14}}/><Link className="button" href="/materials">ПОВТОРИТЬ МАТЕРИАЛ</Link></div></main>}
