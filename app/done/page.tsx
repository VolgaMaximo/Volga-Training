'use client';
export default function DonePage(){const score=typeof window!=='undefined'?Number(sessionStorage.getItem('volga_quiz_score')||0):0;const passed=score>=85;const oralDone=typeof window!=='undefined'?sessionStorage.getItem('volga_oral_done')==='1':false;return <main><div className="brand">VOLGA · RESULTADO</div><h1>{passed?(oralDone?'EXAMEN TERMINADO':'TEST SUPERADO'):'HAY QUE REPASAR'}</h1><div className="card"><div className="score">{score}%</div>{passed?(oralDone?<p className="success">Tus respuestas han sido enviadas para revisión.</p>:<p className="success">Has superado la parte teórica.</p>):<p className="warning">El mínimo es 85%. Repasa las tarjetas y vuelve a intentarlo cuando te lo indique el responsable.</p>}</div></main>}
